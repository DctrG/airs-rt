# Copy this file to .env and adjust values as needed

# Gemini API Key (optional - only needed if NOT running in GCP)
# If running in GCP, Vertex AI will be used automatically via Application Default Credentials
GEMINI_API_KEY=

# Model name (default: gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash

# Vertex AI settings (only needed if forcing Vertex AI mode)
# If GOOGLE_CLOUD_PROJECT is set, Vertex AI will be used automatically
GOOGLE_CLOUD_PROJECT=
VERTEX_LOCATION=us-central1
USE_VERTEX=0

# GCP Project ID (for deployment scripts)
# Set this to avoid passing project-id as argument
# export GOOGLE_CLOUD_PROJECT=your-project-id


